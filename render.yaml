services:
  - type: web
    name: wams
    env: node
    plan: free
    buildCommand: npm install && npm install --prefix server
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true
